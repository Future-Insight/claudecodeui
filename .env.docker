# Claude Code UI Docker Environment Configuration
# This file is used specifically for Docker deployments

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Backend server port (must match Dockerfile EXPOSE)
PORT=3001

# Node environment
NODE_ENV=production

# =============================================================================
# DOCKER-SPECIFIC CONFIGURATION
# =============================================================================

# Data directory (mapped to /app/data in container)
HOME=/app/data

# =============================================================================
# 本地磁盘挂载配置
# =============================================================================

# Claude CLI 配置目录 (宿主机路径，容器内映射到 /app/data/claude)
CLAUDE_CONFIG_DIR=~/.claude

# Claude CLI 主配置文件 (宿主机路径，容器内映射到 /app/.claude.json)
CLAUDE_JSON=~/.claude.json

# 代码工作区目录 (宿主机路径，容器内映射到 /app/workspace)
# 所有代码修改都会保存到这个目录
WORKSPACE_DIR=~/workspace

# Git 全局配置文件 (可选)
GIT_CONFIG=~/.gitconfig

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# API key for additional security (uncomment to enable)
# API_KEY=your-secure-api-key-here

# Database configuration
# SQLITE_DB_PATH=/app/data/auth/auth.db

# =============================================================================
# CLAUDE CLI CONFIGURATION
# =============================================================================

# Claude CLI data directory (mounted from host)
CLAUDE_CONFIG_DIR=/app/data/claude

# 工作区目录配置
WORKSPACE_DIR=/app/workspace

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# Enable request logging
LOG_REQUESTS=true